
services:
  - type: web
    name: ricardo-railmind
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 app.py"
    envVars:
      - key: FLASK_ENV
        value: production
